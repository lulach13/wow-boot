def get_character_info(name, realm):
    # Temporar — fake data
    return f"{name} from {realm} has ilvl 480 (fake data 👀)"

def search_web(query):
    # Temporar — simulăm căutarea
    return f"Search results for: {query}"
